/**
 * ELN Core — Memory barrel.
 *
 * Persistence and (from P1) prose retention + retrieval. Both are adapters —
 * nothing in this directory is required by the narrative core.
 */

export * from './adapters/index.js';
export * from './store.js';
